{
  "version": "2.0.0",
  "tasks": [
    {
      // btw we aren't using local dev setup rn but its nice for next week todos.
      "label": "start supabase local dev env",
      "type": "shell",
      "command": "nix develop && clear",
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "focus": true,
        "close": false
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
